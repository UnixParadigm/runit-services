export TERM='vt100'

USER="minecraft"
GROUPLIST="$(id -Gn "$USER" | tr ' ' ':')"
HOME="/home/${USER}"

SERVERNAME="${PWD##*-}"
LOG="${HOME}/log/${SERVERNAME}-$(date '+%Y-%m-%d_%T').log"
: ${SERVERNAME:="current"}
